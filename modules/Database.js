module.exports = {
    // Insert
    Insert: function(coll, data, callback) {
        // coll.insert(data, (err, doc) => callback(err, doc))
        console.log("Insert function")
    },

    Select: function(coll, data) {
        // db.insert(data)
        console.log("Insert function")
    }
}